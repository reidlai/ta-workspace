// Code generated by goa v3.14.2, DO NOT EDIT.
//
// ta-server HTTP client CLI support package
//
// Command:
// $ goa gen github.com/reidlai/ta-workspace/apps/ta-server/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	exchangec "github.com/reidlai/ta-workspace/apps/ta-server/gen/http/exchange/client"
	insightsc "github.com/reidlai/ta-workspace/apps/ta-server/gen/http/insights/client"
	watchlistc "github.com/reidlai/ta-workspace/apps/ta-server/gen/http/watchlist/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `watchlist (list|add|remove)
insights list
exchange (list|get)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` watchlist list --user-id "Voluptas iure omnis sapiente unde."` + "\n" +
		os.Args[0] + ` insights list --user-id "Ea quia enim voluptas sed."` + "\n" +
		os.Args[0] + ` exchange list --query "Quibusdam expedita pariatur voluptatibus."` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		watchlistFlags = flag.NewFlagSet("watchlist", flag.ContinueOnError)

		watchlistListFlags      = flag.NewFlagSet("list", flag.ExitOnError)
		watchlistListUserIDFlag = watchlistListFlags.String("user-id", "REQUIRED", "")

		watchlistAddFlags      = flag.NewFlagSet("add", flag.ExitOnError)
		watchlistAddBodyFlag   = watchlistAddFlags.String("body", "REQUIRED", "")
		watchlistAddUserIDFlag = watchlistAddFlags.String("user-id", "REQUIRED", "")

		watchlistRemoveFlags      = flag.NewFlagSet("remove", flag.ExitOnError)
		watchlistRemoveSymbolFlag = watchlistRemoveFlags.String("symbol", "REQUIRED", "")
		watchlistRemoveUserIDFlag = watchlistRemoveFlags.String("user-id", "REQUIRED", "")

		insightsFlags = flag.NewFlagSet("insights", flag.ContinueOnError)

		insightsListFlags      = flag.NewFlagSet("list", flag.ExitOnError)
		insightsListUserIDFlag = insightsListFlags.String("user-id", "REQUIRED", "")

		exchangeFlags = flag.NewFlagSet("exchange", flag.ContinueOnError)

		exchangeListFlags     = flag.NewFlagSet("list", flag.ExitOnError)
		exchangeListQueryFlag = exchangeListFlags.String("query", "", "")

		exchangeGetFlags            = flag.NewFlagSet("get", flag.ExitOnError)
		exchangeGetOperatingMicFlag = exchangeGetFlags.String("operating-mic", "REQUIRED", "Operating MIC of the exchange")
	)
	watchlistFlags.Usage = watchlistUsage
	watchlistListFlags.Usage = watchlistListUsage
	watchlistAddFlags.Usage = watchlistAddUsage
	watchlistRemoveFlags.Usage = watchlistRemoveUsage

	insightsFlags.Usage = insightsUsage
	insightsListFlags.Usage = insightsListUsage

	exchangeFlags.Usage = exchangeUsage
	exchangeListFlags.Usage = exchangeListUsage
	exchangeGetFlags.Usage = exchangeGetUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "watchlist":
			svcf = watchlistFlags
		case "insights":
			svcf = insightsFlags
		case "exchange":
			svcf = exchangeFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "watchlist":
			switch epn {
			case "list":
				epf = watchlistListFlags

			case "add":
				epf = watchlistAddFlags

			case "remove":
				epf = watchlistRemoveFlags

			}

		case "insights":
			switch epn {
			case "list":
				epf = insightsListFlags

			}

		case "exchange":
			switch epn {
			case "list":
				epf = exchangeListFlags

			case "get":
				epf = exchangeGetFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "watchlist":
			c := watchlistc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = watchlistc.BuildListPayload(*watchlistListUserIDFlag)
			case "add":
				endpoint = c.Add()
				data, err = watchlistc.BuildAddPayload(*watchlistAddBodyFlag, *watchlistAddUserIDFlag)
			case "remove":
				endpoint = c.Remove()
				data, err = watchlistc.BuildRemovePayload(*watchlistRemoveSymbolFlag, *watchlistRemoveUserIDFlag)
			}
		case "insights":
			c := insightsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = insightsc.BuildListPayload(*insightsListUserIDFlag)
			}
		case "exchange":
			c := exchangec.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = exchangec.BuildListPayload(*exchangeListQueryFlag)
			case "get":
				endpoint = c.Get()
				data, err = exchangec.BuildGetPayload(*exchangeGetOperatingMicFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// watchlistUsage displays the usage of the watchlist command and its
// subcommands.
func watchlistUsage() {
	fmt.Fprintf(os.Stderr, `Manage user watchlist
Usage:
    %[1]s [globalflags] watchlist COMMAND [flags]

COMMAND:
    list: List implements list.
    add: Add implements add.
    remove: Remove implements remove.

Additional help:
    %[1]s watchlist COMMAND --help
`, os.Args[0])
}
func watchlistListUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] watchlist list -user-id STRING

List implements list.
    -user-id STRING: 

Example:
    %[1]s watchlist list --user-id "Voluptas iure omnis sapiente unde."
`, os.Args[0])
}

func watchlistAddUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] watchlist add -body JSON -user-id STRING

Add implements add.
    -body JSON: 
    -user-id STRING: 

Example:
    %[1]s watchlist add --body '{
      "on_hand": false,
      "symbol": "Minima veniam labore tempora sit."
   }' --user-id "Facere asperiores omnis facilis nihil qui doloremque."
`, os.Args[0])
}

func watchlistRemoveUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] watchlist remove -symbol STRING -user-id STRING

Remove implements remove.
    -symbol STRING: 
    -user-id STRING: 

Example:
    %[1]s watchlist remove --symbol "Inventore aut qui eos." --user-id "Est fuga commodi laboriosam recusandae illo."
`, os.Args[0])
}

// insightsUsage displays the usage of the insights command and its subcommands.
func insightsUsage() {
	fmt.Fprintf(os.Stderr, `Provide AI insights
Usage:
    %[1]s [globalflags] insights COMMAND [flags]

COMMAND:
    list: List implements list.

Additional help:
    %[1]s insights COMMAND --help
`, os.Args[0])
}
func insightsListUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] insights list -user-id STRING

List implements list.
    -user-id STRING: 

Example:
    %[1]s insights list --user-id "Ea quia enim voluptas sed."
`, os.Args[0])
}

// exchangeUsage displays the usage of the exchange command and its subcommands.
func exchangeUsage() {
	fmt.Fprintf(os.Stderr, `Manage financial exchanges
Usage:
    %[1]s [globalflags] exchange COMMAND [flags]

COMMAND:
    list: List implements list.
    get: Get implements get.

Additional help:
    %[1]s exchange COMMAND --help
`, os.Args[0])
}
func exchangeListUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] exchange list -query STRING

List implements list.
    -query STRING: 

Example:
    %[1]s exchange list --query "Quibusdam expedita pariatur voluptatibus."
`, os.Args[0])
}

func exchangeGetUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] exchange get -operating-mic STRING

Get implements get.
    -operating-mic STRING: Operating MIC of the exchange

Example:
    %[1]s exchange get --operating-mic "Eligendi eos."
`, os.Args[0])
}
