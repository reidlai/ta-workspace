language: 'typescript'
type: 'library'
id: 'svelte-demo'

dependsOn:
  - 'ts-types'

tasks:
  build:
    command: 'pnpm run build'
    inputs:
      - 'src/**/*'
      - 'tsconfig.json'
    outputs:
      - 'dist'
