$schema: 'https://moonrepo.dev/schemas/project.json'
id: 'demo-svelte'
type: 'library'
stack: 'frontend'

tasks:
  build:
    outputs: []
    options:
      mergeOutputs: 'replace'

  dev:
    command: 'vite dev'
    local: true

  storybook:
    command: 'storybook dev -p 6006'
    local: true
