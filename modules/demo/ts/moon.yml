$schema: 'https://moonrepo.dev/schemas/project.json'
id: 'demo-ts'
type: 'library'
stack: 'frontend'

tasks:
  build:
    outputs:
      - 'dist'
  lint:
    command: 'eslint .'
    options:
      mergeArgs: 'replace'

  test:
    command: 'vitest run'

  format:
    command: 'prettier --write .'
