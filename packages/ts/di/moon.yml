language: 'typescript'
type: 'library'
id: 'ts-di'

dependsOn:
  - 'ts-types'

tasks:
  build:
    command: 'pnpm run build'
    inputs:
      - 'src/**/*'
      - 'tsconfig.json'
    outputs:
      - 'dist'
