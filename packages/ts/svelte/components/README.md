# Svelte Components Package

This package will contain shared Svelte components that can be used across multiple Svelte applications in the monorepo.

## Structure

```
components/
├── ui/           # Basic UI components
├── forms/        # Form components
└── layouts/      # Layout components
```

## Usage

```typescript
import { Button } from '@svelte/components';
```
